name: LARI Program V2 Phase 5 Disposable Postgres Acceptance & Live Security Matrix

on:
  push:
    branches:
      - ci/phase6-node3-pos-cart-checkout-acceptance-r1
      - aos/phase6-node3-pos-cart-checkout
      - ci/phase6-node2-procurement-receiving-acceptance-r1
      - aos/phase6-node2-suppliers-po-receiving
      - ci/phase6-node1-product-inventory-acceptance-r1
      - ci/phase5-disposable-postgres-acceptance-r5
      - ci/phase5-disposable-postgres-acceptance-r4
      - ci/phase5-disposable-postgres-acceptance-r3
      - ci/phase5-disposable-postgres-acceptance-r2
      - ci/phase5-disposable-postgres-acceptance
      - ci/program-v2-p3p4-live-behavioral-acceptance-r1-20260911-01
      - ci/program-v2-p3p4-live-behavioral-acceptance-20260911-01
      - ci/program-v2-p3p4-postgres-acceptance-20260911-01
  workflow_dispatch:

permissions:
  contents: read

jobs:
  postgres-acceptance:
    name: Clean Disposable Supabase Postgres Replay & Live Behavioral Acceptance
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Evidence Composition
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Database Tools (postgresql-client)
        run: |
          sudo apt-get update -y
          sudo apt-get install -y postgresql-client
          psql --version

      - name: Start Disposable Pinned Supabase Postgres Container
        run: |
          PINNED_IMAGE="supabase/postgres:15.1.0.147"
          echo "Pulling pinned Supabase Postgres container: ${PINNED_IMAGE}"
          docker pull "${PINNED_IMAGE}"
          docker run -d \
            --name lari-disposable-postgres \
            -e POSTGRES_PASSWORD=postgres \
            -e POSTGRES_DB=postgres \
            -p 54322:5432 \
            "${PINNED_IMAGE}"

      - name: Wait for Postgres Readiness
        run: |
          DB_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
          for i in {1..60}; do
            if pg_isready -h 127.0.0.1 -p 54322 -U postgres -d postgres; then
              echo "Postgres container ready!"
              break
            fi
            sleep 1
          done
          psql "$DB_URL" -c "SELECT version();"

      - name: Execute Managed Runtime Compatibility Bootstrap
        run: |
          DB_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
          psql "$DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/fixtures/p2a_managed_runtime_bootstrap.sql
          echo "Managed runtime bootstrap completed."

      - name: Apply Full Ordered SQL Migration Chain
        id: replay_migrations
        run: |
          set -o pipefail
          DB_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
          mapfile -t MIGRATIONS < <(find supabase/migrations -maxdepth 1 -type f -name '*.sql' -printf '%f\n' | sort)
          echo "Total migrations found: ${#MIGRATIONS[@]}"
          ORDINAL=1
          for migration in "${MIGRATIONS[@]}"; do
            if [ "${migration}" = "20260813_h1c_commercial_eligibility_and_quota_enforcement.sql" ]; then
              echo "=== Applying Historical Replay Compatibility Bridge before Migration 38 ==="
              psql "$DB_URL" -v ON_ERROR_STOP=1 -f "supabase/tests/fixtures/p2a_historical_replay_bridge_before_migration_38.sql"
            fi
            echo "=== APPLYING [${ORDINAL}/${#MIGRATIONS[@]}] ${migration} ==="
            psql "$DB_URL" -v ON_ERROR_STOP=1 -f "supabase/migrations/${migration}"
            ORDINAL=$((ORDINAL + 1))
          done
          echo "ALL ${#MIGRATIONS[@]} MIGRATIONS SUCCESSFULLY APPLIED WITH ZERO ERRORS!"

      - name: Install Node Dependencies
        run: |
          npm ci

      - name: Execute 23 Domain Contract Suites (Phase 2, 3, 4, Phase 5 Nodes 1-6 + Phase 6 Nodes 1-3)
        run: |
          set -euo pipefail
          npx tsx scripts/test-phase2-staff-scheduling-contracts.mjs
          npx tsx scripts/test-phase3-resource-capacity-contracts.mjs
          npx tsx scripts/test-phase3-package-multibranch-contracts.mjs
          npx tsx scripts/test-phase3-waitlist-contracts.mjs
          npx tsx scripts/test-phase3-communications-contracts.mjs
          npx tsx scripts/test-phase3-payment-foundation-contracts.mjs
          npx tsx scripts/test-phase3-background-job-calendar-contracts.mjs
          npx tsx scripts/test-phase3-customer360-segmentation-contracts.mjs
          npx tsx scripts/test-phase3-reporting-analytics-contracts.mjs
          npx tsx scripts/test-phase3-custom-domain-contracts.mjs
          npx tsx scripts/test-phase4-deposit-noshow-contracts.mjs
          npx tsx scripts/test-phase4-packages-memberships-contracts.mjs
          npx tsx scripts/test-phase4-giftcards-wallet-contracts.mjs
          npx tsx scripts/test-phase4-loyalty-contracts.mjs
          npx tsx scripts/test-phase5-vertical-skus-contracts.mjs
          npx tsx scripts/test-phase5-clinic-practitioner-contracts.mjs
          npx tsx scripts/test-phase5-ht-journey-contracts.mjs
          npx tsx scripts/test-phase5-node4-clinic-ai-contracts.mjs
          npx tsx scripts/test-phase5-node5-funnel-contracts.mjs
          npx tsx scripts/test-phase5-node6-lead-ops-contracts.mjs
          npx tsx scripts/test-phase6-node1-product-inventory-contracts.mjs
          npx tsx scripts/test-phase6-node2-suppliers-po-receiving-contracts.mjs
          npx tsx scripts/test-phase6-node3-pos-mixed-cart-checkout-contracts.mjs
          echo "ALL 23 DOMAIN CONTRACT SUITES PASSED!"

      - name: Execute Live Database Introspection & Integrity Verification
        run: |
          DB_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
          echo "=== VERIFYING FUNCTION INTEGRITY ==="
          psql "$DB_URL" -c "
            SELECT routine_name, routine_type, security_type
            FROM information_schema.routines
            WHERE routine_schema = 'public'
              AND routine_name IN (
                'create_public_booking',
                'evaluate_booking_slot',
                'evaluate_and_lock_resource_plan',
                'claim_outbox_batch',
                'enqueue_communication_outbox',
                'record_delivery_callback',
                'get_tenant_communication_outbox',
                'bind_payment_intent_provider',
                'claim_background_job_batch',
                'evaluate_booking_confirmation_deposit_policy',
                'scan_customer_reactivation_cohorts',
                'get_customer_loyalty_profile',
                'earn_loyalty_points_for_appointment',
                'resolve_tenant_vertical_context',
                'clinic_get_my_context',
                'clinic_set_staff_profile',
                'ht_create_treatment_journey',
                'ht_create_or_update_journey_quote',
                'ht_add_journey_itinerary_event',
                'pos_create_product',
                'pos_record_stock_receipt',
                'pos_decrement_stock_for_sale',
                'pos_adjust_stock',
                'pos_return_restock',
                'pos_get_branch_inventory',
                'pos_create_supplier',
                'pos_create_purchase_order',
                'pos_approve_purchase_order',
                'pos_cancel_purchase_order',
                'pos_receive_purchase_order_items',
                'pos_get_purchase_order',
                'pos_create_order',
                'pos_add_cart_item',
                'pos_checkout_order',
                'pos_get_order'
              )
            ORDER BY routine_name;
          "

          echo "=== VERIFYING TABLE AND CONSTRAINT INTEGRITY ==="
          psql "$DB_URL" -c "
            SELECT conname, contype, conrelid::regclass
            FROM pg_constraint
            WHERE conname IN (
              'fk_appointment_resources_appointment_tenant',
              'fk_appointment_resources_resource_tenant',
              'uq_payment_intents_provider_reference',
              'communication_outbox_channel_check',
              'communication_outbox_status_check',
              'fk_appointment_deposits_appointment_tenant',
              'fk_appointment_deposits_payment_intent_tenant',
              'uq_loyalty_tenant_idempotency',
              'uq_ht_treatment_journeys_id_tenant',
              'uq_ht_journey_quotes_id_tenant',
              'uq_ht_journey_itinerary_events_id_tenant',
              'uq_products_tenant_sku',
              'uq_inventory_balances_branch_product',
              'uq_inventory_movements_idempotency',
              'uq_suppliers_tenant_name',
              'uq_purchase_orders_tenant_po_number',
              'chk_po_items_received_bound',
              'uq_pore_tenant_item_idempotency',
              'uq_pos_orders_tenant_order_number',
              'uq_pos_orders_tenant_idempotency',
              'chk_pos_items_product_ref'
            )
            ORDER BY conname;
          "

      - name: Execute Live PostgreSQL Behavioral Acceptance Matrix
        run: |
          set -o pipefail
          DB_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
          echo "=== EXECUTING LIVE POSTGRESQL BEHAVIORAL MATRIX (ALL 15 DOMAINS + EV079-R4) ==="
          node supabase/tests/program-v2/p3p4-live/test-live-behavioral-matrix.mjs 2>&1 | tee matrix.log
          STATUS="${PIPESTATUS[0]}"
          if [ "$STATUS" -ne 0 ]; then
            echo "=== STEP 12 BEHAVIORAL MATRIX FAILED (EXIT CODE: $STATUS) - DUMPING MATRIX.LOG ==="
            cat matrix.log || true
            exit "$STATUS"
          fi
          echo "=== BEHAVIORAL MATRIX COMPLETED CLEANLY ==="

      - name: Execute Phase 5 Live PostgreSQL Security & Concurrency Matrix
        run: |
          set -o pipefail
          DB_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
          echo "=== EXECUTING PHASE 5 NODE 2 & NODE 3 LIVE POSTGRESQL SECURITY MATRIX ==="
          node supabase/tests/program-v2/p5-live/test-phase5-postgres-security-matrix.mjs 2>&1 | tee -a matrix.log
          STATUS="${PIPESTATUS[0]}"
          if [ "$STATUS" -ne 0 ]; then
            echo "=== STEP 13 SECURITY MATRIX FAILED (EXIT CODE: $STATUS) - DUMPING MATRIX.LOG ==="
            cat matrix.log || true
            exit "$STATUS"
          fi
          echo "=== PHASE 5 SECURITY & CONCURRENCY MATRIX COMPLETED CLEANLY ==="

      - name: Execute Phase 6 Node 1 Live PostgreSQL Inventory Behavioral Matrix
        run: |
          set -o pipefail
          DB_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
          echo "=== EXECUTING PHASE 6 NODE 1 LIVE POSTGRESQL INVENTORY BEHAVIORAL MATRIX ==="
          node supabase/tests/program-v2/phase6-live/test-phase6-node1-inventory-behavioral-matrix.mjs 2>&1 | tee -a matrix.log
          STATUS="${PIPESTATUS[0]}"
          if [ "$STATUS" -ne 0 ]; then
            echo "=== STEP 14 PHASE 6 INVENTORY MATRIX FAILED (EXIT CODE: $STATUS) - DUMPING MATRIX.LOG ==="
            cat matrix.log || true
            exit "$STATUS"
          fi
          echo "=== PHASE 6 NODE 1 INVENTORY BEHAVIORAL MATRIX COMPLETED CLEANLY ==="

      - name: Execute Phase 6 Node 2 Live PostgreSQL Procurement & Receiving Behavioral Matrix
        run: |
          set -o pipefail
          DB_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
          echo "=== EXECUTING PHASE 6 NODE 2 LIVE POSTGRESQL PROCUREMENT & RECEIVING BEHAVIORAL MATRIX ==="
          node supabase/tests/program-v2/phase6-live/test-phase6-node2-procurement-receiving-behavioral-matrix.mjs 2>&1 | tee -a matrix.log
          STATUS="${PIPESTATUS[0]}"
          if [ "$STATUS" -ne 0 ]; then
            echo "=== STEP 15 PHASE 6 PROCUREMENT MATRIX FAILED (EXIT CODE: $STATUS) - DUMPING MATRIX.LOG ==="
            cat matrix.log || true
            exit "$STATUS"
          fi
          echo "=== PHASE 6 NODE 2 PROCUREMENT & RECEIVING BEHAVIORAL MATRIX COMPLETED CLEANLY ==="

      - name: Execute Phase 6 Node 3 Live PostgreSQL POS Cart & Checkout Behavioral Matrix
        run: |
          set -o pipefail
          DB_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
          echo "=== EXECUTING PHASE 6 NODE 3 LIVE POSTGRESQL POS CART & CHECKOUT BEHAVIORAL MATRIX ==="
          node supabase/tests/program-v2/phase6-live/test-phase6-node3-pos-cart-checkout-behavioral-matrix.mjs 2>&1 | tee -a matrix.log
          STATUS="${PIPESTATUS[0]}"
          if [ "$STATUS" -ne 0 ]; then
            echo "=== STEP 16 PHASE 6 POS CART & CHECKOUT MATRIX FAILED (EXIT CODE: $STATUS) - DUMPING MATRIX.LOG ==="
            cat matrix.log || true
            exit "$STATUS"
          fi
          echo "=== PHASE 6 NODE 3 POS CART & CHECKOUT BEHAVIORAL MATRIX COMPLETED CLEANLY ==="

      - name: Application Build
        run: |
          set -euo pipefail
          echo "=== RUNNING APPLICATION BUILD ==="
          npm run build

      - name: Typecheck & Lint
        run: |
          set -euo pipefail
          echo "=== RUNNING TYPECHECK & LINT ==="
          npm run lint

      - name: Dependency Security Audit Classification
        run: |
          echo "=== RUNNING DEPENDENCY SECURITY AUDIT (AUDIT ONLY - NO MUTATION) ==="
          npm audit --json > npm-audit-report.json || true
          node -e "
            const fs = require('fs');
            const data = JSON.parse(fs.readFileSync('npm-audit-report.json', 'utf8'));
            const vulns = data.vulnerabilities || {};
            const summary = {
              total: Object.keys(vulns).length,
              direct: 0,
              transitive: 0,
              dev: 0,
              runtime: 0,
              fixAvailable: 0
            };
            for (const [name, v] of Object.entries(vulns)) {
              if (v.isDirect) summary.direct++; else summary.transitive++;
              if (v.dev) summary.dev++; else summary.runtime++;
              if (v.fixAvailable) summary.fixAvailable++;
            }
            console.log('DEPENDENCY AUDIT CLASSIFICATION SUMMARY:');
            console.log(JSON.stringify(summary, null, 2));
          "

      - name: Upload Failure Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: acceptance-diagnostics
          path: matrix.log
          if-no-files-found: ignore

      - name: Destroy Container
        if: always()
        run: |
          docker rm -f lari-disposable-postgres || true
