# EV-079-R1 Evidence: Phase 4 Loyalty & Client Reactivation Foundation R1 Corrections

- **Authority Directive**: `LARI-PROGRAM-V2-PHASE3-FINAL-CORRECTIONS-AND-PHASE4-INTEGRATION-20260911-01`
- **Candidate Branch**: `aos/phase4-loyalty-reactivation-foundation-r1`
- **Base SHA**: `b95b1827efc200cd5d701a2de50793bc0d72ada2`
- **Commit SHA**: `997e2eee9bb4957685a3eab025b9f0ab0563ede2`
- **Remote Push**: `origin/aos/phase4-loyalty-reactivation-foundation-r1` verified.
- **Production Status**: `NO_GO`

---

## R1 Corrections Implemented

1. **Caller Authorization & Scope in Read RPC**:
   - `public.get_customer_loyalty_profile` hardened with caller authorization check against `public.users_profile` (`super_admin`, `tenant_owner`, `staff` with tenant scope matching `p_tenant_id`).
   - Authenticated callers cannot query arbitrary tenant or customer loyalty profiles.

2. **Appointment Verification in Point Earning**:
   - `public.earn_loyalty_points_for_appointment` verifies:
     - Appointment exists.
     - Appointment belongs to exact `p_tenant_id`.
     - Appointment belongs to exact `p_customer_id`.
     - Appointment `status = 'completed'`.
   - Derives reward basis from accepted canonical catalog source (`services.price`), avoiding arbitrary caller-supplied financial amounts.

3. **Composite Foreign Keys & Search Path**:
   - Added tenant composite foreign keys across `customer_loyalty_ledger`, `customer_loyalty_balances`, and `customer_reactivation_events`.
   - Hardened `SET search_path = pg_catalog, public` across all functions.

4. **Append-Only Loyalty Ledger**:
   - Added `trg_prevent_loyalty_ledger_mutation` trigger on `public.customer_loyalty_ledger` forbidding updates and deletes.

5. **Reactivation Cohort Scanning RPC**:
   - Implemented `public.scan_customer_reactivation_cohorts(p_tenant_id, p_inactivity_days)` detecting inactive customer cohorts (60/90 days) from completed appointments.
   - Explicitly classified outbox dispatch hook as `QUEUED_FOR_COMMUNICATIONS_BOUNDARY` reusing EV057 communications infrastructure without direct network sends.

---

## Contract Test Evidence

- Executed `scripts/test-phase4-loyalty-contracts.mjs`:
  - 16 passed, 0 failed.
- Zero network send, zero external provider credentials.
