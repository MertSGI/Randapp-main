# EV-057-R5: Provider-Neutral Communications Foundation R5 Corrections

- **Authority**: `LARI-PROGRAM-V2-P3P4-FINAL-SOURCE-CORRECTIONS-POSTGRES-ACCEPTANCE-PHASE5-CONTINUATION-20260911-01`
- **Program**: `LARI-PROGRAM-V2-REAL-PRODUCT-20260908-01`
- **Branch**: `aos/phase3-communications-foundation-r5`
- **Commit**: `788901610961160839cde630533e0ea65815f1d8`
- **Parent / Base**: `010bce92525ebd4eadf36e757df0a772a3564da1`
- **Remote Push**: `origin/aos/phase3-communications-foundation-r5` verified.
- **Production Status**: `NO_GO`

---

## Key Corrections & Invariant Enforcement

1. **Targeted Legacy Constraint Reconciliation**:
   - Replaced wildcard discovery (`conname LIKE '%channel%' OR conname LIKE '%status%'`) with exact constraint targeting:
     ```sql
     conname IN ('communication_outbox_channel_check', 'communication_outbox_status_check')
     ```
   - Guarantees `UNRELATED_CHECK_CONSTRAINT_PRESERVATION = REQUIRED`. All unrelated CHECK constraints are preserved.

2. **Explicit Tenant Type Consistency**:
   - `communication_outbox.tenant_id` is TEXT. Explicitly normalized comparisons across queries:
     - `WHERE tenant_id = p_tenant_id::text AND idempotency_key = v_clean_idempotency`
     - `WHERE co.tenant_id = p_tenant_id::text`
   - Eliminates dependence on nonexistent implicit text/uuid equality in PostgreSQL.

3. **Canonical User Profile Model Rebinding**:
   - Rebound staff/owner authorization checks from noncanonical `public.user_profiles` to canonical `public.users_profile`:
     ```sql
     SELECT 1 FROM public.users_profile up
     WHERE up.id = auth.uid()
       AND up.active = true
       AND (up.tenant_id = p_tenant_id OR up.role = 'super_admin')
     ```
